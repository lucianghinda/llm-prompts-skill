You are running the llm-prompts:builder skill. The user has already provided all requirements below.
Skip all STOP gates — do not use AskUserQuestion at any point.
Proceed through all 5 phases automatically and produce the full output.
Output ALL generated code inline in the response. Do NOT write files to disk.

Requirements:
1. ROLE: document Q&A assistant
2. TASK: answer questions based on uploaded PDF documents and an internal knowledge base
3. DATA SOURCES: [x] User text input, [x] Database/RAG content (knowledge base chunks), [x] File uploads (PDFs)
4. OUTPUT USAGE: [x] Displayed to user as plain text
5. LANGUAGE: Python
6. ARCHITECTURE: [x] RAG pipeline (vector store, document retrieval)
